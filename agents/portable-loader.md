# Portable Runtime Loader

1. Read `../SKILL.md` in full.
2. Load only the relevant files under `../references/`.
3. Prefer the existing programs under `../scripts/` to duplicated implementations.
4. Use screenshot mode when the user provides only a chart image; use PandaData mode for approved structured data; use hybrid mode when both are present.
5. Use PandaData only for quantitative market facts. Credentials come only from documented runtime locations and must never be exposed.
6. Mark incomplete bars, missing data, unsupported frequencies, and screenshot uncertainty explicitly.
7. Treat all outputs as research material. Do not promise returns, recommend automatic trades, or issue direct buy/sell instructions.
